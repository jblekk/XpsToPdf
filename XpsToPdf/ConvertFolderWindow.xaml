<Window x:Class="XpsToPdf.ConvertFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Convert Folder" Height="220" Width="500" WindowStartupLocation="CenterOwner">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
      <Label Content="Source folder:" VerticalAlignment="Center" Width="100"/>
      <TextBox x:Name="TxtSourceFolder" Width="300" />
      <Button x:Name="BtnBrowseSource" Content="Browse..." Margin="8,0,0,0" Click="BtnBrowseSource_Click"/>
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
      <Label Content="Output folder:" VerticalAlignment="Center" Width="100"/>
      <TextBox x:Name="TxtOutputFolder" Width="300" />
      <Button x:Name="BtnBrowseOutput" Content="Browse..." Margin="8,0,0,0" Click="BtnBrowseOutput_Click"/>
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,8">
      <Button x:Name="BtnStart" Content="Convert" Width="90" Click="BtnStart_Click"/>
      <Button x:Name="BtnCancel" Content="Close" Width="90" Margin="8,0,0,0" Click="BtnCancel_Click"/>
    </StackPanel>

    <TextBlock x:Name="StatusText" Grid.Row="3" Text="Ready" TextWrapping="Wrap" />
  </Grid>
</Window>
